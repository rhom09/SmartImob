---
phase: 06-notificacoes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "backend/package.json",
  "backend/prisma/schema.prisma",
  "backend/src/services/notification.service.ts",
  "backend/src/services/email.service.ts",
  "backend/src/jobs/alerts.job.ts",
  "backend/src/routes/notificacoes.ts",
  "backend/src/server.ts",
  "frontend/src/components/Navigation.tsx",
  "frontend/src/components/NotificationBell.tsx",
  "frontend/src/app/notificacoes/preferencias/page.tsx"
]
autonomous: true
requirements: [RF007.1, RF007.2, RF007.3]
user_setup:
  - service: resend
    why: "Envio de emails transacionais"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys"

must_haves:
  truths:
    - "O sistema gera alertas automáticos de vencimento de contrato"
    - "O usuário recebe emails de notificação via Resend"
    - "A interface exibe um sino de notificações com badge de não lidas"
    - "O usuário pode configurar quais notificações deseja receber por email"
  artifacts:
    - path: "backend/src/jobs/alerts.job.ts"
      provides: "Processamento diário de alertas"
    - path: "backend/src/services/email.service.ts"
      provides: "Integração com Resend e React Email"
    - path: "frontend/src/components/NotificationBell.tsx"
      provides: "Interface de notificações em tempo real"
  key_links:
    - from: "backend/src/jobs/alerts.job.ts"
      to: "backend/src/services/notification.service.ts"
      via: "Chamada de criação de alerta"
    - from: "frontend/src/components/NotificationBell.tsx"
      to: "backend/src/routes/notificacoes.ts"
      via: "Fetch API / realtime subscription"
---

<objective>
Implementar o Sistema de Notificações (Fase 6), abrangendo automação de alertas (contratos, inadimplência), envio de emails transacionais com Resend, central de notificações in-app e gestão de preferências do usuário.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-notificacoes/06-RESEARCH.md
@backend/prisma/schema.prisma
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Infraestrutura e Schema de Dados</name>
  <files>backend/package.json, backend/prisma/schema.prisma, backend/.env.example</files>
  <behavior>
    - Adicionar dependências: resend, node-cron no backend.
    - Adicionar devDependencies: react-email, @react-email/components no backend.
    - Atualizar enum TipoAlerta: adicionar INADIMPLENCIA, INTERESSADO, CONTRATO.
    - Atualizar modelo Alert: tornar contratoId opcional, adicionar link (String?), usuarioId (String?).
    - Criar modelo NotificationPreference: userId, tipo (TipoAlerta), emailEnabled (Boolean), pushEnabled (Boolean).
  </behavior>
  <action>
    1. Instalar pacotes via npm no diretório backend.
    2. Modificar o arquivo prisma/schema.prisma com as novas definições.
    3. Executar npx prisma migrate dev --name add_notification_preferences.
    4. Adicionar RESEND_API_KEY ao .env.example.
  </action>
  <verify>
    <automated>npx prisma validate && npm list resend node-cron</automated>
  </verify>
  <done>Dependências instaladas e banco de dados atualizado com suporte a preferências e novos tipos de alertas.</done>
</task>

<task type="auto">
  <name>Task 2: Serviços de Notificação e Email (Backend)</name>
  <files>backend/src/services/email.service.ts, backend/src/services/notification.service.ts, backend/src/jobs/alerts.job.ts, backend/src/server.ts</files>
  <action>
    1. Implementar EmailService usando a SDK do Resend. Criar template básico com React Email em `src/templates/emails/DefaultNotification.tsx`.
    2. Implementar NotificationService com métodos:
       - `createAlert(data)`: persiste no DB e opcionalmente envia email se a preferência do usuário permitir.
       - `listByUser(userId)`: busca alertas ativos.
       - `markAsRead(alertId)`: atualiza status para LIDO.
    3. Implementar `backend/src/jobs/alerts.job.ts`:
       - Agendar via node-cron para rodar diariamente.
       - Consultar contratos com dataFim em 30, 15, 7 dias.
       - Consultar recibos pendentes após o vencimento (inadimplência).
       - Gerar alertas para cada caso encontrado.
    4. Inicializar o cron job no `server.ts`.
  </action>
  <verify>
    <automated>npm test (se houver testes de serviço) ou log de inicialização do cron no server startup</automated>
  </verify>
  <done>Lógica de geração de alertas e envio de emails operacional.</done>
</task>

<task type="auto">
  <name>Task 3: API e Integração Frontend</name>
  <files>backend/src/routes/notificacoes.ts, frontend/src/components/NotificationBell.tsx, frontend/src/app/notificacoes/preferencias/page.tsx</files>
  <action>
    1. Criar rotas no backend:
       - GET `/api/notificacoes`: Lista alertas do usuário logado.
       - PATCH `/api/notificacoes/:id/ler`: Marca como lido.
       - GET/PUT `/api/notificacoes/preferencias`: Gerencia NotificationPreference.
    2. No Frontend:
       - Criar `NotificationBell.tsx` com Lucide `Bell` icon, dropdown de mensagens rápidas e badge de contagem.
       - Integrar com o backend para polling (ou Supabase Realtime se configurado).
       - Criar página de Preferências para habilitar/desabilitar tipos de notificação por canal.
    3. Adicionar o sino de notificações no `Navigation.tsx`.
  </action>
  <verify>
    <automated>curl -X GET http://localhost:3001/api/notificacoes (mock auth) retorna 200</automated>
  </verify>
  <done>Interface de notificações integrada e funcional, permitindo ao usuário gerenciar alertas e preferências.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → Resend | Envio de dados do usuário (nome, email) para serviço externo. |
| User → Preferences | Configuração de notificações via API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Email Service | mitigate | Usar variáveis de ambiente seguras para API Keys; Validar emails antes de enviar. |
| T-06-02 | Denial of Service | Cron Job | mitigate | Garantir que o job não processe volumes massivos de uma vez (batching em queries do Prisma). |
| T-06-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint para pacotes novos. |
</threat_model>

<verification>
- Verificar se o cron job executa sem erros no log.
- Confirmar recebimento de email de teste via Resend.
- Validar se a badge de notificação atualiza ao marcar como lida.
</verification>

<success_criteria>
- Alertas automáticos são gerados no banco de dados conforme as regras de negócio (datas).
- Emails são disparados apenas se o usuário tiver a preferência habilitada.
- Sino de notificações reflete o estado real do banco de dados (não lidas).
</success_criteria>

<output>
Create `.planning/phases/06-notificacoes/06-01-SUMMARY.md` when done
</output>
