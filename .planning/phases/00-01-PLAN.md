---
phase: 00-setup-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "package.json",
  "frontend/package.json",
  "backend/package.json",
  "frontend/tailwind.config.ts",
  "frontend/app/layout.tsx",
  "prisma/schema.prisma",
  "backend/src/server.ts"
]
autonomous: true
requirements: [RF001.1, RF001.2, RNF001, RNF002, RNF003]
must_haves:
  truths:
    - "A monorepo structure is established with separate frontend and backend directories."
    - "Next.js 14 frontend is configured with Tailwind CSS and Corporate Modern theme."
    - "Node.js backend is configured with Express/Fastify and Prisma."
    - "Supabase project is linked and Auth is configured."
    - "Initial deployment to Vercel and Render is ready."
  artifacts:
    - path: "frontend/tailwind.config.ts"
      provides: "Design system color and typography configuration"
    - path: "prisma/schema.prisma"
      provides: "Initial database schema for Users and Sessions"
    - path: "frontend/app/layout.tsx"
      provides: "Base layout with fixed sidebar following DESIGN.md"

---

<objective>
Establish the project foundation including monorepo setup, technology stack configuration, design system integration, and initial infrastructure for SmartImob.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monorepo and Project Scaffolding</name>
  <files>package.json, frontend/package.json, backend/package.json, .gitignore</files>
  <action>
    - Initialize a monorepo structure using NPM workspaces.
    - Setup Frontend: Next.js 14 (App Router) with TypeScript, Tailwind CSS, and shadcn/ui.
    - Setup Backend: Node.js with Express (or Fastify), TypeScript, and Prisma ORM.
    - Configure shared ESLint, Prettier, and Husky for code quality.
  </action>
  <verify>
    <automated>npm run check-structure</automated>
  </verify>
  <done>Workspaces are correctly linked and build commands work for both frontend and backend.</done>
</task>

<task type="auto">
  <name>Task 2: Design System and Base Layout</name>
  <files>frontend/tailwind.config.ts, frontend/app/layout.tsx, frontend/components/Sidebar.tsx</files>
  <action>
    - Configure Tailwind colors (Navy: #0A192F, Indigo: #3730A3) and typography (Inter/Syne) per DESIGN.md.
    - Implement the base layout with a fixed Sidebar and Top Header.
    - Use modern corporate styling with subtle borders and clean typography.
  </action>
  <verify>
    <automated>npm run lint --prefix frontend</automated>
  </verify>
  <done>Layout matches prototypes in PrototipoTelas/ and colors follow branding guidelines.</done>
</task>

<task type="auto">
  <name>Task 3: Database and Auth Integration</name>
  <files>prisma/schema.prisma, frontend/lib/supabase.ts, backend/src/middleware/auth.ts</files>
  <action>
    - Configure Prisma schema with User and Session models.
    - Integrate Supabase Auth on the frontend (Client SDK) for Email/Password and Google OAuth.
    - Implement a JWT-based authentication middleware on the backend to validate Supabase tokens.
    - Setup environment variables for database connection and Supabase keys.
  </action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Prisma schema is valid, and Supabase client is initialized with environment variables.</done>
</task>

<task type="auto">
  <name>Task 4: Deployment and CI/CD</name>
  <files>vercel.json, .github/workflows/deploy.yml</files>
  <action>
    - Configure Vercel for frontend deployment.
    - Configure Render (or similar) for backend deployment.
    - Setup basic GitHub Actions for CI (linting, type checking).
  </action>
  <verify>
    <automated>gh secret list</automated>
  </verify>
  <done>Deployment configurations are in place and CI pipeline is ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted JSON data over HTTPS |
| API → Supabase | Secure connection via service role / JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|-----------|-----------|-------------|-----------------|
| T-00-01 | Spoofing | Auth Service | mitigate | Use Supabase Auth with JWT and secure cookies |
| T-00-02 | Information Disclosure | Environment Variables | mitigate | Store secrets in Vercel/Render dashboard, never in git |
| T-00-SC | Tampering | npm installs | mitigate | Use package-lock.json and regular audit |
</threat_model>

<success_criteria>
- Monorepo structure established.
- Frontend and Backend are running locally.
- Base layout implemented with corporate design.
- Supabase Auth integrated.
- Deployment pipelines configured.
</success_criteria>

<output>
Create `.planning/phases/00-setup-infrastructure/00-01-PLAN.md` when done
</output>
