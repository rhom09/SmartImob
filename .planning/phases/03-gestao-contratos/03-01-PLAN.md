---
phase: 03-gestao-contratos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/prisma/schema.prisma, backend/src/controllers/contractController.ts, backend/src/routes/contractRoutes.ts, backend/src/services/contractService.ts]
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03]
must_haves:
  truths:
    - "Criação de contrato gera parcelas automáticas no banco de dados"
    - "Status do imóvel muda para OCUPADO ao ativar contrato"
    - "Endpoints de CRUD de contratos operacionais"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Modelos Contract, Receipt e Adjustment"
    - path: "backend/src/services/contractService.ts"
      provides: "Lógica de geração de parcelas"
  key_links:
    - from: "Contract"
      to: "Receipt"
      via: "Geração automática no service"
---

<objective>
Configurar a base de dados para contratos e implementar o backend inicial com a lógica de automação de parcelas.

Propósito: Garantir que a estrutura de dados suporte o ciclo de vida do contrato e reduza o trabalho manual do usuário através da geração de parcelas.
Output: Schema atualizado e API de contratos com automação de recibos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/prisma/schema.prisma
@DER.md
@PrototipoTelas/contratos.png
@PrototipoTelas/recibos.jpeg
</context>

<tasks>

<task type="auto">
  <name>Atualizar Schema e Migrations</name>
  <files>backend/prisma/schema.prisma</files>
  <action>Validar se os campos do modelo Contract e Receipt no schema.prisma estão alinhados com o DER.md. Adicionar campos de auditoria (createdAt, updatedAt) se faltarem. Executar `npx prisma migrate dev` para atualizar o banco.</action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Banco de dados atualizado com tabelas CONTRATOS, RECIBOS e REAJUSTES.</done>
</task>

<task type="auto" tdd="true">
  <name>Implementar Service de Contratos com Geração de Parcelas</name>
  <files>backend/src/services/contractService.ts</files>
  <behavior>
    - Criar contrato: deve salvar no banco e retornar o objeto.
    - Se finalidade for LOCACAO: deve calcular o número de meses entre dataInicio e dataFim e criar um registro em Receipt para cada mês, respeitando o diaVencimento.
    - Atualizar Imóvel: ao criar contrato, o status da Property vinculada deve passar para OCUPADO.
  </behavior>
  <action>Implementar a lógica de criação de contrato. Use a biblioteca `date-fns` para manipulação de datas e cálculo de parcelas mensais.</action>
  <verify>
    <automated>npm test backend/tests/contractService.test.ts</automated>
  </verify>
  <done>Lógica de negócio para criação de contrato e parcelas concluída.</done>
</task>

<task type="auto">
  <name>Implementar Endpoints de Contrato</name>
  <files>backend/src/routes/contractRoutes.ts, backend/src/controllers/contractController.ts</files>
  <action>Criar rotas GET (listagem com filtros), GET/:id (detalhes), POST (cadastro) e DELETE (encerramento). Vincular ao service criado.</action>
  <verify>
    <automated>curl -X GET http://localhost:3001/api/contratos</automated>
  </verify>
  <done>API de contratos pronta para consumo pelo frontend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Validação de datas e valores monetários na criação do contrato |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Contract Creation | mitigate | Validar com Zod se dataFim > dataInicio e se o imóvel não está OCUPADO. |
| T-03-02 | Denial of Service | Receipt Generation | mitigate | Limitar o número máximo de parcelas geradas (ex: max 120 meses/10 anos). |
</threat_model>

<verification>
Executar migrações, gerar client do prisma e testar a criação de um contrato via Postman/Curl verificando se as parcelas foram criadas na tabela RECIBOS.
</verification>

<success_criteria>
- Schema Prisma reflete o DER.md.
- POST /api/contratos cria o contrato e as parcelas vinculadas.
- Status do imóvel é atualizado para OCUPADO automaticamente.
</success_criteria>

<output>
Create `.planning/phases/03-gestao-contratos/03-01-SUMMARY.md` when done
</output>
