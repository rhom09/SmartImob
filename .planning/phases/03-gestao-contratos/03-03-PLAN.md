---
phase: 03-gestao-contratos
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [backend/src/controllers/contractController.ts, backend/src/services/contractService.ts, frontend/src/app/contratos/[id]/page.tsx]
autonomous: true
requirements: [CONT-02.1]
must_haves:
  truths:
    - "Sistema permite registrar reajustes de valor no contrato"
    - "Histórico de reajustes é visível para o usuário"
---

<objective>
Implementar a funcionalidade de reajustes anuais de contratos.

Propósito: Garantir a manutenção financeira dos contratos de longo prazo conforme índices de mercado.
Output: Sistema de reajuste funcional com histórico.
</objective>

<tasks>

<task type="auto">
  <name>Implementar Lógica de Reajuste no Backend</name>
  <files>backend/src/services/contractService.ts, backend/src/controllers/contractController.ts</files>
  <action>
    - Adicionar método `applyAdjustment` no `ContractService`.
    - Este método deve: 
        1. Criar registro na tabela `Adjustment`.
        2. Atualizar `valorAluguel` na tabela `Contract`.
        3. (Opcional) Atualizar parcelas futuras que ainda estão PENDENTES.
    - Adicionar rota POST `/api/contratos/:id/reajustar`.
  </action>
  <done>Backend suporta aplicação de reajustes.</done>
</task>

<task type="auto">
  <name>Interface de Reajuste no Frontend</name>
  <files>frontend/src/app/contratos/[id]/page.tsx</files>
  <action>
    - Adicionar botão "Aplicar Reajuste" na página de detalhes.
    - Criar formulário simples (ou modal) para informar índice (IGP-M, IPCA, etc) e novo valor.
    - Exibir lista de reajustes anteriores no card de informações.
  </action>
  <done>Usuário pode aplicar e visualizar reajustes no frontend.</done>
</task>

</tasks>

<success_criteria>
- Aplicar um reajuste altera o valor do aluguel do contrato.
- O histórico de reajustes mostra o valor anterior e o novo valor corretamente.
</success_criteria>
