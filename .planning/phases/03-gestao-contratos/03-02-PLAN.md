---
phase: 03-gestao-contratos
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [backend/src/controllers/contractController.ts, backend/src/services/contractService.ts, frontend/src/app/contratos/[id]/page.tsx]
autonomous: true
requirements: [CONT-02, FIN-01]
must_haves:
  truths:
    - "Usuário pode dar baixa em recibos (pagamento)"
    - "Sistema gera PDF de recibo formatado"
  artifacts:
    - path: "backend/src/controllers/receiptController.ts"
      provides: "Endpoints de baixa e PDF"
    - path: "frontend/src/components/contratos/ReceiptActions.tsx"
      provides: "Interface de pagamento e download"
---

<objective>
Implementar a gestão financeira dos contratos, permitindo a baixa de pagamentos e a geração de documentos PDF.

Propósito: Completar o ciclo operacional do contrato de locação, permitindo o controle de caixa e a emissão de comprovantes.
Output: Sistema de baixa de parcelas e gerador de PDF funcional.
</objective>

<tasks>

<task type="auto">
  <name>Implementar Controller e Rotas de Recibos</name>
  <files>backend/src/controllers/receiptController.ts, backend/src/routes/receiptRoutes.ts</files>
  <action>
    - Criar `receiptController.ts` com métodos `update` (para baixa de pagamento) e `generatePDF`.
    - Criar `receiptRoutes.ts` e registrar no `server.ts`.
    - Implementar lógica de atualização de status do recibo no banco.
  </action>
  <done>Endpoints de recibos operacionais.</done>
</task>

<task type="auto">
  <name>Implementar Geração de PDF (Recibo)</name>
  <files>backend/src/services/pdfService.ts</files>
  <action>
    - Instalar biblioteca `pdfkit` ou `jspdf` (servidor).
    - Criar template de recibo de aluguel contendo: Logotipo (placeholder), Dados do Contrato, Valor, Referência, Inquilino e Proprietário.
    - Endpoint para streaming do PDF para o cliente.
  </action>
  <done>PDF de recibo gerado dinamicamente.</done>
</task>

<task type="auto">
  <name>Interface de Baixa e Ações no Frontend</name>
  <files>frontend/src/app/contratos/[id]/page.tsx</files>
  <action>
    - Adicionar modal de "Confirmar Pagamento" na página de detalhes do contrato.
    - Adicionar botão de "Download PDF" na linha de cada recibo.
    - Feedback visual de sucesso/erro ao realizar pagamento.
  </action>
  <done>Usuário consegue interagir com os recibos no frontend.</done>
</task>

</tasks>

<success_criteria>
- Clicar em "Pagar" altera o status do recibo para PAGO no banco e reflete na UI.
- Clicar em "Baixar Recibo" gera e abre um PDF formatado com os dados da parcela.
</success_criteria>
