---
phase: 01-gestao-imoveis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/src/server.ts
  - frontend/src/app/imoveis/page.tsx
  - frontend/src/app/imoveis/novo/page.tsx
  - frontend/src/app/imoveis/[id]/page.tsx
autonomous: true
requirements:
  - RF002.1
  - RF002.2
  - RF002.3
  - RF002.4
  - RF002.5

must_haves:
  truths:
    - "Usuário pode cadastrar um imóvel com todos os campos obrigatórios e fotos"
    - "Usuário pode listar imóveis com filtros de tipo, finalidade e preço"
    - "Usuário pode visualizar os detalhes completos de um imóvel, incluindo galeria de fotos"
    - "Usuário pode editar as informações de um imóvel existente"
    - "Usuário pode excluir um imóvel (soft delete)"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Schema completo de Imóveis, Endereços e Fotos"
    - path: "backend/src/routes/imoveis.ts"
      provides: "Endpoints de CRUD e Upload de Fotos"
    - path: "frontend/src/app/imoveis/page.tsx"
      provides: "Listagem de imóveis com filtros e paginação"
    - path: "frontend/src/components/imoveis/PropertyForm.tsx"
      provides: "Formulário de cadastro/edição com Zod e React Hook Form"
  key_links:
    - from: "frontend/src/app/imoveis/novo/page.tsx"
      to: "backend/src/routes/imoveis.ts"
      via: "POST /api/imoveis"
    - from: "frontend/src/components/imoveis/ImageUpload.tsx"
      to: "Supabase Storage"
      via: "supabase.storage.from('imoveis').upload()"
---

<objective>
Implementar o módulo completo de gestão de imóveis, abrangendo desde a persistência de dados no PostgreSQL (via Prisma) até a interface de usuário no Next.js, incluindo gestão de arquivos no Supabase Storage e integração com ViaCEP.

Propósito: Core do negócio (gestão do inventário imobiliário).
Output: CRUD completo de imóveis funcional com galeria de fotos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@backend/prisma/schema.prisma
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atualizar Schema do Prisma e Criar Migration</name>
  <files>backend/prisma/schema.prisma</files>
  <behavior>
    - Adicionar campos detalhados ao modelo Property (PropertyType, Purpose, Status enums)
    - Adicionar modelo Address relacionado (CEP, Rua, Numero, Bairro, Cidade, Estado)
    - Adicionar modelo PropertyImage relacionado (URL, isMain, order)
    - Garantir que Property tenha soft delete (deletedAt)
  </behavior>
  <action>
    Atualizar o arquivo schema.prisma conforme RF002.1.
    Campos sugeridos:
    - Enums: PropertyType (CASA, APTO, TERRENO, COMERCIAL), Purpose (VENDA, LOCACAO, AMBOS), PropertyStatus (DISPONIVEL, ALUGADO, VENDIDO, NEGOCIACAO, INATIVO).
    - Property: areaTotal, areaConstruida, quartos, suites, banheiros, vagas, valorVenda, valorLocacao, condominio, iptu, descricao, observacoesInternas, deletedAt.
    Executar `npx prisma migrate dev --name add_property_details`.
  </action>
  <verify>
    <automated>npx prisma validate && ls backend/prisma/migrations/*/migration.sql</automated>
  </verify>
  <done>Schema atualizado e migration gerada com sucesso.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar Backend API (CRUD + Soft Delete)</name>
  <files>backend/src/routes/imoveis.ts, backend/src/controllers/PropertyController.ts, backend/src/server.ts</files>
  <action>
    Criar rotas e controller para gestão de imóveis.
    - GET /api/imoveis: Listagem com paginação e filtros (Prisma query).
    - POST /api/imoveis: Cadastro com validação Zod.
    - GET /api/imoveis/:id: Detalhes.
    - PUT /api/imoveis/:id: Atualização.
    - DELETE /api/imoveis/:id: Soft delete (set deletedAt).
    Registrar rotas no server.ts.
  </action>
  <verify>
    <automated>npm --prefix backend test</automated>
  </verify>
  <done>Endpoints funcionais e validados com Zod.</done>
</task>

<task type="auto">
  <name>Task 3: Implementar UI de Listagem e Filtros</name>
  <files>frontend/src/app/imoveis/page.tsx, frontend/src/components/imoveis/PropertyFilters.tsx, frontend/src/components/imoveis/PropertyCard.tsx</files>
  <action>
    Criar a página de listagem de imóveis seguindo o design Corporativo (Navy/Indigo).
    - Implementar componente de Filtros (Tipo, Finalidade, Preço).
    - Implementar Grid/Lista de Cards (exibindo foto de capa, valor, endereço e características).
    - Adicionar paginação.
    - Usar Tailwind CSS para responsividade.
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <done>Página de listagem funcional e responsiva.</done>
</task>

<task type="auto">
  <name>Task 4: Implementar Formulário de Cadastro com ViaCEP e Upload</name>
  <files>frontend/src/app/imoveis/novo/page.tsx, frontend/src/components/imoveis/PropertyForm.tsx, frontend/src/components/imoveis/ImageUpload.tsx</files>
  <action>
    Criar formulário completo de cadastro/edição.
    - Integrar React Hook Form + Zod para validação.
    - Implementar trigger no campo CEP para buscar endereço via https://viacep.com.br/ws/{cep}/json/.
    - Implementar ImageUpload integrando com Supabase Storage (bucket 'imoveis').
    - Garantir tratamento de erros e loading states.
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <done>Formulário de cadastro funcional com integração ViaCEP e upload de fotos.</done>
</task>

<task type="auto">
  <name>Task 5: Implementar Detalhes do Imóvel e Galeria</name>
  <files>frontend/src/app/imoveis/[id]/page.tsx, frontend/src/components/imoveis/PropertyGallery.tsx</files>
  <action>
    Criar página de visualização detalhada.
    - Exibir galeria de fotos com lightbox (ou carrossel).
    - Exibir todas as informações técnicas organizada por seções (Valores, Localização, Características).
    - Adicionar ações de Editar e Excluir.
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <done>Página de detalhes completa com galeria de fotos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Validação de payload com Zod e autenticação via Middleware |
| API -> Supabase Storage | Upload de arquivos (limitar tipos mime e tamanho) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Property Update | mitigate | Validar ownership/role no controller antes de permitir UPDATE/DELETE |
| T-01-02 | Information Disclosure | Property Details | mitigate | RLS no Supabase para garantir que apenas usuários autenticados vejam detalhes internos |
| T-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint para pacotes novos |
</threat_model>

<verification>
- [ ] Cadastro de imóvel salva corretamente no banco.
- [ ] Fotos são enviadas para o bucket do Supabase.
- [ ] Busca por CEP preenche automaticamente os campos de endereço.
- [ ] Soft delete oculta o imóvel da listagem mas mantém no banco.
- [ ] Listagem filtra corretamente por tipo e faixa de preço.
</verification>

<success_criteria>
Módulo de Gestão de Imóveis operacional, permitindo o fluxo completo de cadastro, visualização, edição e exclusão de propriedades com suporte a fotos.
</success_criteria>

<output>
Create `.planning/phases/01-gestao-imoveis/01-01-PLAN-SUMMARY.md` when done
</output>
