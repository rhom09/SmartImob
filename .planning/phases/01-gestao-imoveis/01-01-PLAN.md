---
phase: 01-gestao-imoveis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/src/server.ts
  - frontend/src/app/imoveis/page.tsx
  - frontend/src/app/imoveis/novo/page.tsx
  - frontend/src/app/imoveis/[id]/page.tsx
autonomous: true
requirements:
  - RF002.1
  - RF002.2
  - RF002.3
  - RF002.4
  - RF002.5

must_haves:
  truths:
    - "Usuário pode cadastrar um proprietário com dados validados (CPF/CNPJ único)"
    - "Usuário pode cadastrar um imóvel vinculado a um proprietário existente"
    - "Usuário pode listar imóveis com filtros de tipo, status e localização"
    - "Usuário pode visualizar os detalhes completos de um imóvel, incluindo dados do proprietário"
    - "O banco de dados segue a estrutura de tabelas em português (ou mapeada) conforme o DER"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Schema atualizado com PROPRIETARIOS (Owner) e IMOVEIS (Property) conforme DER"
    - path: "backend/src/routes/imoveis.ts"
      provides: "Endpoints de CRUD para Imóveis e Proprietários"
    - path: "frontend/src/app/imoveis/page.tsx"
      provides: "Listagem de imóveis com filtros e paginação"
    - path: "frontend/src/components/imoveis/PropertyForm.tsx"
      provides: "Formulário de cadastro/edição integrado com Proprietários e ViaCEP"
  key_links:
    - from: "backend/prisma/schema.prisma"
      to: "PostgreSQL"
      via: "Prisma Migrate"
      pattern: "model Owner|model Property"
    - from: "frontend/src/app/imoveis/novo/page.tsx"
      to: "backend/src/routes/imoveis.ts"
      via: "POST /api/imoveis"
---

<objective>
Implementar os módulos de Gestão de Proprietários e Imóveis conforme definido no DER.md, garantindo a integridade referencial entre as entidades e seguindo as nomenclaturas padronizadas.

Propósito: Core do negócio (gestão do inventário imobiliário e seus donos).
Output: CRUD completo de Proprietários e Imóveis funcional com integração de endereço.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@DER.md
@backend/prisma/schema.prisma
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atualizar Schema do Prisma (Proprietários e Imóveis) conforme DER</name>
  <files>backend/prisma/schema.prisma</files>
  <behavior>
    - Criar modelo Owner (Proprietários) com campos: id, name, cpfCnpj (unique), phone, email, address, status, createdAt, updatedAt.
    - Atualizar modelo Property (Imóveis) com campos: id, ownerId (FK), type, address, number, complement, neighborhood, city, state, zipCode, description, status (Vago, Ocupado, Suspenso), createdAt, updatedAt.
    - Usar @map no Prisma para garantir nomes de tabelas e colunas em português no banco (ex: @@map("PROPRIETARIOS"), @map("proprietario_id")).
  </behavior>
  <action>
    Refatorar o schema.prisma para refletir fielmente o DER.md.
    1. Criar model Owner (@@map("PROPRIETARIOS")).
    2. Atualizar model Property (@@map("IMOVEIS")).
    3. Garantir que a relação 1:N entre Proprietário e Imóvel esteja correta.
    Executar `npx prisma migrate dev --name sync_schema_with_der`.
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma migrate status</automated>
  </verify>
  <done>Schema sincronizado com o DER e migrations aplicadas.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar Backend API para Proprietários e Imóveis</name>
  <files>backend/src/routes/proprietarios.ts, backend/src/routes/imoveis.ts, backend/src/server.ts</files>
  <action>
    Criar controladores e rotas para ambas as entidades.
    - CRUD Proprietários: POST, GET (list/detail), PUT, DELETE.
    - CRUD Imóveis: POST (exigindo proprietario_id), GET (com filtros), PUT, DELETE.
    - Validar campos obrigatórios e formatos (CPF/CNPJ) usando Zod.
    Registrar rotas no server.ts.
  </action>
  <verify>
    <automated>npm --prefix backend test</automated>
  </verify>
  <done>API REST funcional para Proprietários e Imóveis.</done>
</task>

<task type="auto">
  <name>Task 3: Implementar UI de Gestão de Proprietários</name>
  <files>frontend/src/app/proprietarios/page.tsx, frontend/src/components/proprietarios/OwnerForm.tsx</files>
  <action>
    Criar interface para cadastro e listagem de proprietários.
    - Listagem com busca por nome/CPF.
    - Formulário de cadastro com validação Zod e máscaras de entrada.
    - Integração com a nova API de proprietários.
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <done>Módulo de proprietários funcional no frontend.</done>
</task>

<task type="auto">
  <name>Task 4: Atualizar UI de Imóveis com Seleção de Proprietário e ViaCEP</name>
  <files>frontend/src/app/imoveis/novo/page.tsx, frontend/src/components/imoveis/PropertyForm.tsx</files>
  <action>
    Atualizar o formulário de imóveis.
    - Adicionar campo de seleção (Select/Autocomplete) para vincular um Proprietário.
    - Implementar busca de endereço via API ViaCEP ao digitar o CEP.
    - Sincronizar campos com os novos nomes do DER (bairro, cidade, estado, etc).
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <done>Formulário de imóveis atualizado e integrado.</done>
</task>

<task type="auto">
  <name>Task 5: Implementar Listagem e Detalhes de Imóveis</name>
  <files>frontend/src/app/imoveis/page.tsx, frontend/src/app/imoveis/[id]/page.tsx</files>
  <action>
    Finalizar as telas de visualização.
    - Listagem de imóveis com filtros por status (Vago/Ocupado) e tipo.
    - Página de detalhes exibindo informações do imóvel e do proprietário vinculado.
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <done>Visualização de imóveis completa e integrada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Validação rigorosa de proprietario_id para evitar vinculação órfã |
| Database | Constraint de Unique no CPF/CNPJ de proprietários |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Relation Property-Owner | mitigate | Validar no backend se o proprietario_id existe antes de criar o imóvel |
| T-01-02 | Information Disclosure | PII Proprietário | mitigate | Garantir que dados sensíveis de proprietários só sejam acessíveis por usuários autorizados |
</threat_model>

<verification>
- [ ] Proprietário cadastrado aparece na lista de seleção do imóvel.
- [ ] Schema do banco reflete os nomes definidos no DER.md.
- [ ] Não é possível cadastrar imóvel sem proprietário válido.
- [ ] ViaCEP preenche corretamente os campos de endereço no formulário de imóveis.
</verification>

<success_criteria>
Módulos de Proprietários e Imóveis operacionais e integrados conforme o DER, permitindo o fluxo completo de gestão imobiliária base.
</success_criteria>

<output>
Create `.planning/phases/01-gestao-imoveis/01-01-PLAN-SUMMARY.md` when done
</output>
